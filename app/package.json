{
  "name": "nenemi-app",
  "version": "1.0.0",
  "private": true,
  "description": "Native shell for Nenemi. Loads www.mynenemi.com inside a Capacitor WebView.",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.5.1",
    "@capacitor/core": "^8.5.1",
    "@capacitor/ios": "^8.5.1"
  }
}
